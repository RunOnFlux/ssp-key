/**
 * Images should be stored in the `App/Images` directory and referenced using variables defined here.
 *
 * Dark theme must use the white logo variant (DESIGN_TOKENS.md §2 — pick the
 * logo variant by background, never hardcode one file).
 */

export default function () {
  return {
    logo: require('../../assets/images/ssp-logo-white.png'),
  };
}
